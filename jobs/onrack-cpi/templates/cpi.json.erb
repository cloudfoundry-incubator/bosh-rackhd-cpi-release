<%=

JSON.dump(
    "apiserver" => p("onrack-cpi.apiserver"),

    "agent" => {
      "blobstore" => p("onrack-cpi.agent.blobstore"),
      "mbus" => p("onrack-cpi.agent.mbus"),
      "ntp"  => p("onrack-cpi.agent.ntp"),
    },

    "max_create_vm_attempts" => p("onrack-cpi.max_create_vm_attempts", 5),
    "run_workflow_timeout" => p("onrack-cpi.run_workflow_timeout", 1200),
)
%>
