<%=

JSON.dump(
  "Actions" => {
    "User" => p("baremetal_cpi.user"),
    "Port" => p("baremetal_cpi.port"),
    "PrivateKey" => p("baremetal_cpi.private_key"),

    "Machines" => p("baremetal_cpi.machines"),

    "StemcellsDir" => p("baremetal_cpi.stemcells_dir"),

    "Agent" => {
      "Mbus" => p("baremetal_cpi.agent.mbus"),
      "NTP"  => p("baremetal_cpi.agent.ntp"),

      "Blobstore" => {
        "Type"    => p("baremetal_cpi.agent.blobstore.provider"),
        "Options" => p("baremetal_cpi.agent.blobstore.options"),
      },
    },
  },
)
%>
